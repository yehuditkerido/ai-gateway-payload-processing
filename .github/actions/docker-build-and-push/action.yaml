name: Docker Build and Push
description: Build multi-platform image and push to GitHub Container Registry

inputs:
  image-name:
    required: true
    description: Image name (without registry prefix)
  tag:
    required: true
    description: Image tag
  registry:
    required: true
    description: Container registry (e.g., ghcr.io/opendatahub-io)
  platform:
    required: false
    default: "linux/amd64"
    description: Target platform (e.g., linux/amd64, linux/arm64)
  github-token:
    required: true
    description: GitHub token for registry authentication
  prerelease:
    required: false
    default: "false"
    description: If true, skip tagging as 'latest'
  dockerfile:
    required: false
    default: "Dockerfile"
    description: Path to Dockerfile

runs:
  using: composite
  steps:
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v4

    - name: Login to GitHub Container Registry
      shell: bash
      run: echo "${{ inputs.github-token }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Extract build metadata
      id: meta
      shell: bash
      run: |
        echo "commit_sha=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"
        echo "build_ref=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"

    - name: Extract arch suffix
      id: arch
      shell: bash
      run: |
        # Extract arch from platform (e.g., linux/amd64 -> amd64)
        ARCH="${{ inputs.platform }}"
        ARCH="${ARCH##*/}"
        echo "suffix=$ARCH" >> "$GITHUB_OUTPUT"

    - name: Build and push
      shell: bash
      run: |
        ARCH_SUFFIX="${{ steps.arch.outputs.suffix }}"
        TAGS="-t ${{ inputs.registry }}/${{ inputs.image-name }}:${{ inputs.tag }}-${ARCH_SUFFIX}"
        
        if [[ "${{ inputs.prerelease }}" != "true" ]]; then
          TAGS="$TAGS -t ${{ inputs.registry }}/${{ inputs.image-name }}:latest-${ARCH_SUFFIX}"
        fi
        
        docker buildx build \
          --platform ${{ inputs.platform }} \
          --cache-from type=gha,scope=${{ inputs.image-name }}-${{ inputs.platform }} \
          --cache-to type=gha,mode=max,scope=${{ inputs.image-name }}-${{ inputs.platform }} \
          --build-arg COMMIT_SHA=${{ steps.meta.outputs.commit_sha }} \
          --build-arg BUILD_REF=${{ steps.meta.outputs.build_ref }} \
          $TAGS \
          -f ${{ inputs.dockerfile }} \
          --push .

    - name: Print image info
      shell: bash
      run: |
        ARCH_SUFFIX="${{ steps.arch.outputs.suffix }}"
        echo "Pushed: ${{ inputs.registry }}/${{ inputs.image-name }}:${{ inputs.tag }}-${ARCH_SUFFIX}"
        if [[ "${{ inputs.prerelease }}" != "true" ]]; then
          echo "Pushed: ${{ inputs.registry }}/${{ inputs.image-name }}:latest-${ARCH_SUFFIX}"
        fi
